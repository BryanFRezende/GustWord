import test as gw

appid = gw.getAppID()

runFlag = True
while runFlag = 
flag = input("Choose which operation you would like to run:\n  Run all cities by typing [all].\n. Run one or a list of cities by typing [select].")

if flag
weather_at_city = gw.getCityWeather(833, "string", appid)

print(jsonafied)